{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "ModuleSwitchRequest",
  "description": "Request to switch from one module to another at runtime",
  "type": "object",
  "properties": {
    "old_module": {
      "type": "string",
      "description": "ID of the current module to switch from"
    },
    "new_module": {
      "type": "string",
      "description": "ID of the new module to switch to"
    },
    "reason": {
      "type": "string",
      "description": "Reason for the switch"
    },
    "requested_by": {
      "type": "string",
      "description": "Identifier of who requested the switch (e.g., 'agent-123' or 'user')"
    },
    "force": {
      "type": "boolean",
      "description": "Skip compatibility checks if true",
      "default": false
    },
    "timestamp": {
      "type": "integer",
      "description": "Unix timestamp of the request"
    }
  },
  "required": ["old_module", "new_module", "reason", "requested_by", "timestamp"]
}
