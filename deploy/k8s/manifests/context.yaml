apiVersion: apps/v1
kind: Deployment
metadata:
  name: wireframe-context
  namespace: wireframe-ai
  labels:
    app: wireframe-context
spec:
  replicas: 1
  selector:
    matchLabels:
      app: wireframe-context
  template:
    metadata:
      labels:
        app: wireframe-context
    spec:
      containers:
        - name: context
          image: wireframe-ai-context:latest
          env:
            - name: WIREFRAME_AI_NATS_URL
              value: "nats://nats:4222"
            - name: RUST_LOG
              value: "info"
          resources:
            requests:
              memory: "64Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "500m"
          livenessProbe:
            exec:
              command:
                - /bin/sh
                - -c
                - "pgrep -f 'wireframe-ai-context' > /dev/null"
            initialDelaySeconds: 30
            periodSeconds: 30
